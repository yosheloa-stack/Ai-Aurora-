{
  "name": "ai-aurora",
  "version": "1.0.0",
  "description": "Uma IA própria: rede neural recorrente (RNN) treinada do zero em TypeScript, sem frameworks de machine learning.",
  "main": "dist/train.js",
  "scripts": {
    "train": "ts-node src/train.ts",
    "generate": "ts-node src/generate.ts",
    "search": "ts-node src/search.ts",
    "tools": "ts-node src/tools-cli.ts",
    "server": "ts-node src/api/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
